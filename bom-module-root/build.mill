package build

import mill.*
import mill.javalib.*

object `package` extends BomModule {
  def bomMvnDeps = Seq(
    mvn"com.google.protobuf:protobuf-bom:4.28.1"
  )
  def depManagement = Seq(
    mvn"io.grpc:grpc-protobuf:1.67.1"
  )
}
